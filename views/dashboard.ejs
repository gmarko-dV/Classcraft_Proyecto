<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mi Classcraft - Dashboard</title>
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/dashboard">Dashboard</a></li>
        <li><a href="/logout">Cerrar sesión</a></li>
      </ul>
    </nav>
  </header>

  <section class="user-info">
    <h1>Bienvenido, <%= user.first_name %> <%= user.last_name %></h1>
    <p>Rol: <%= user.role %></p>
    <p>Oro: <%= user.gold %></p>
    <p>Personaje: <%= user.character || 'Sin personaje asignado' %></p>
    
    <% if (user.character && user.character !== 'Sin personaje asignado') { %>
      <img src="/images/<%= user.character.toLowerCase() %>.png" alt="<%= user.character %>" width="150">
    <% } else { %>
      <p>Aún no has seleccionado un personaje.</p>
    <% } %>
  </section>

  <section class="user-actions">
    <h2>Acciones disponibles</h2>
    <ul>
      <li><a href="/tasks">Mis tareas</a></li>
      <li><a href="/courses">Mis cursos</a></li>
      <li><a href="/grades">Mis calificaciones</a></li>
    </ul>
  </section>

  <footer>
    <p>&copy; 2025 Mi Classcraft. Todos los derechos reservados.</p>
  </footer>
</body>
</html>
