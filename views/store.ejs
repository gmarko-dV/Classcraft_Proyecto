<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mi Classcraft - Tienda</title>
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/dashboard">Dashboard</a></li>
        <li><a href="/logout">Cerrar sesión</a></li>
      </ul>
    </nav>
  </header>

  <section class="store">
    <h1>Bienvenido a la Tienda</h1>
    
    <div class="items">
      <% articulos.forEach(item => { %>
        <div class="item">
          <img src="/images/<%= item.url_imagen %>" alt="<%= item.nombre %>" width="150">
          <p><%= item.nombre %></p>
          <p>Precio: <%= item.precio %> de oro</p>
          <form action="/comprar" method="POST">
            <input type="hidden" name="id_estudiante" value="<%= user.id %>">
            <input type="hidden" name="id_articulo" value="<%= item.id %>">
            <button type="submit">Comprar</button>
          </form>
        </div>
      <% }); %>
    </div>
  </section>
</body>
</html>
